{
  "name": "saucedemo-cypress-tests",
  "version": "1.0.0",
  "description": "Testes automatizados do SauceDemo com Cypress",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "cy:run:headless": "cypress run --headless --browser chrome"
  },
  "devDependencies": {
    "cypress": "^13.6.0"
  }
}
